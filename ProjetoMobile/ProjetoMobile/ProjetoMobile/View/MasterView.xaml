<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ProjetoMobile.ViewModel"
             Style="{StaticResource backgroundColorDefault}"
             Title="Menu"
             x:Class="ProjetoMobile.View.MasterView">
    <StackLayout Margin="0, 10, 0, 10">
        <StackLayout VerticalOptions="FillAndExpand">
            <Button Text="Meu carrinho" Command="{Binding CommandMeuCarrinho}" Style="{StaticResource buttonDefault}"></Button>
            <Button Text="Meus pedidos" Command="{Binding CommandMeusPedidos}" Style="{StaticResource buttonDefault}"></Button>
        </StackLayout>
        <StackLayout VerticalOptions="End">
            <Button Text="Sair" Command="{Binding CommandSair}" Style="{StaticResource buttonDefault}"></Button>
        </StackLayout>        
    </StackLayout>
</ContentPage>